<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shop">
	<select id="shop_list" parameterType="int" resultType="dto.FoodsDTO" >
	select * from fb_foods where category_no = #{category_no} order by foods_no desc
	</select>
	
	<select id="count" resultType="int" parameterType="dto.FoodsDTO">
		select count(*) from foods where category_no = #{category_no}
	</select>
	
	<select id="pagelist" resultType="dto.FoodsDTO" parameterType="dto.shop_PageDTO">
		<![CDATA[
		select b.* from(
		select rownum as rm, a.* from(
		select * from fb_foods )a)b
		where b.rm>=#{startRow} and b.rm <=#{endRow}
		]]>
	</select>

</mapper>

